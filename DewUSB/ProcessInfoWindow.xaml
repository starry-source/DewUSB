<ui:FluentWindow xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DewUSB"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        x:Class="DewUSB.ProcessInfoWindow"
        Title="进程管理" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowCornerPreference="Round"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ui:TitleBar Title="进程管理（实验）"/>

        <ui:DynamicScrollViewer Grid.Row="1" Padding="16">
            <Grid>
				<TextBlock>如果这个能正常功能工作，请务必告诉我！这对我很重要。</TextBlock>
				
                <StackPanel x:Name="ProcessPanel" Margin="0,20,0,0">
                </StackPanel>
                
                <StackPanel x:Name="NoProcessText" 
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Visibility="Collapsed">
                    <ui:SymbolIcon Symbol="Info24" 
                                 HorizontalAlignment="Center"
                                 Margin="0,0,0,8"/>
                    <TextBlock Text="找不到占用进程" 
                             HorizontalAlignment="Center"
                             TextAlignment="Center"/>
                    <TextBlock Text="这是很正常，handle似乎只能用于本地磁盘" 
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                             Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </ui:DynamicScrollViewer>

        <Grid Grid.Row="2" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ui:Button Grid.Column="1"
                      Content="关闭"
                      Click="CloseButton_Click"
                      Margin="8,0,0,0"/>
        </Grid>
    </Grid>
</ui:FluentWindow>